const switcherDuration = Duration(milliseconds: 250);

const httpTimeout = Duration(seconds: 10);

const mockDuration = Duration(milliseconds: 1500);

const jsonUrl =
    'https://raw.githubusercontent.com/sizovsv/test-assignment/main/json/contacts/json';
